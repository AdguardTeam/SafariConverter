/**
 * Extension global preferences stub
 */

adguard.prefs = (function (adguard) {
    var Prefs = {
        speedupStartup: function () {
            return false;
        }
    };

    return Prefs;
})(adguard);
